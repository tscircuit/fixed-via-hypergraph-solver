{
  "name": "@tscircuit/fixed-via-hypergraph-solver",
  "version": "0.0.1",
  "main": "dist/index.js",
  "type": "module",
  "files": [
    "dist"
  ],
  "scripts": {
    "start": "cosmos",
    "build": "tsup ./lib/index.ts --dts --format esm",
    "test": "bun test",
    "generate:via-topology": "bun scripts/generate-via-topology.ts",
    "generate:via-traces": "bun scripts/generate-via-traces-svg.ts",
    "generate:baked-via-tiles": "bun scripts/generate-baked-via-tiles.ts",
    "generate:via-assets": "bun scripts/generate-via-assets.ts",
    "format": "biome format --write .",
    "format:check": "biome format .",
    "benchmark:dataset02": "bun scripts/benchmark-dataset02.ts",
    "benchmark:dataset02:smoke": "bun scripts/benchmark-dataset02.ts --limit=20 --concurrency=2",
    "build:site": "cosmos-export"
  },
  "dependencies": {
    "@tscircuit/find-convex-regions": "^0.1.3",
    "@tscircuit/hypergraph": "^0.0.71",
    "graphics-debug": "^0.0.86"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.11",
    "@types/bun": "latest",
    "react-cosmos": "^7.1.0",
    "react-cosmos-plugin-vite": "^7.1.0",
    "tsup": "^8.5.1",
    "typescript": "^5.9.3"
  }
}
